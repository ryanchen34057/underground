package enums;

public enum Id {
    wall, player, coin,
    upwardSpike, downwardSpike, rightwardSpike, leftwardSpike,
    landingEffect, bluePortal, torch, breakableWall, dashInTheAirEffect, verticalDashEffect,dashEffect
}
